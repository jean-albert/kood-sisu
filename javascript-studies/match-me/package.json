{
  "name": "m",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "all": "chmod +x setup.sh && bash setup.sh",
    "dev:redis": "docker compose up -d redis",
    "dev:db": "docker compose up -d db",
    "dev?old": "npm run dev:db && concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev": "npm run dev:db && npm run dev:redis && concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && go run main.go",
    "dev:frontend": "cd frontend && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@hookform/resolvers": "^5.0.1",
    "formik": "^2.4.6",
    "yup": "^1.6.1"
  }
}
